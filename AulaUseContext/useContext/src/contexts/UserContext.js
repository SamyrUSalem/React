import { createContext } from "react";

//Essa função irá criar o contexto(será o local onde os dados serão armazenados para poderem ser compartilhados com outros componentes)
const UserContext = createContext()

export default UserContext